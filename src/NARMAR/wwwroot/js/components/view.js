loader(function (ngModule) {
    ngModule.directive('narmarView', ($compile, $http, $mdDialog) => ({
        link: function (scope,element,attrs) {
            /*** Data ***/

            /*** Functions ***/

            /*** Main ***/
            
        }
    }));
});